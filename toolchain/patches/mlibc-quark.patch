diff --git a/meson.build b/meson.build
index 8e981a3c..55f15372 100644
--- a/meson.build
+++ b/meson.build
@@ -324,6 +324,8 @@ elif host_machine.system() == 'ethereal'
 elif host_machine.system() == 'demo'
        subdir('sysdeps/demo')
 # ANCHOR_END: demo-sysdeps
+elif host_machine.system() == 'quark'
+       subdir('sysdeps/quark')
 else
        error('No sysdeps defined for OS: ' + host_machine.system())
 endif
